-- 08 · NÃO USE para o caso do Michel Bidri
-- A sessão marcada como "não realizada" ESTÁ CORRETA (de fato não aconteceu).
-- Este arquivo existia por um mal-entendido e não deve ser executado.
-- Se no futuro precisar reabrir outra sessão por engano, peça um script novo
-- com data + nome da sessão específicos.
SELECT 'Não execute este script. A sessão do Bidri permanece como não realizada.' AS aviso;
